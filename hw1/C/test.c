#include <stdio.h>

int main()
{
    int in_user,in_movie;
    printf("Enter user:");
    scanf("%d", &in_user);
    printf("Enter movie:");
    scanf("%d", &in_movie);

    printf("User: %d Movie: %d\n", in_user,in_movie);
    return 0;
}
